# Step 4：前端登录注册

> 目标：实现首页、登录页、注册页，以及统一的 API 请求封装。用户能注册、登录并跳转到仪表盘。

---

## 4.1 要实现的文件

```
frontend/src/
├── app/
│   ├── layout.tsx
│   ├── globals.css
│   ├── page.tsx              # 首页
│   ├── login/
│   │   └── page.tsx
│   └── register/
│       └── page.tsx
├── lib/
│   ├── api.ts                # API 封装
│   └── utils.ts
```

---

## 4.2 API 封装 (lib/api.ts)

所有前端请求的统一入口，负责处理请求头、Token、错误处理等核心逻辑。


```typescript
const API_BASE = process.env.NEXT_PUBLIC_API_URL || "http://localhost:8000";

export class ApiError extends Error {
  constructor(public status: number, message: string) {
    super(message);
    this.name = "ApiError";
  }
}

export async function fetchApi(url: string, options: RequestInit & { data?: any } = {}) {
  const { data, ...rest } = options;
  const token = typeof window !== "undefined" ? localStorage.getItem("token") : "";

  const headers: Record<string, string> = {
    ...(token && { Authorization: `Bearer ${token}` }),
    ...(options.headers as Record<string, string>),
  };
  if (data && !(data instanceof FormData)) {
    headers["Content-Type"] = "application/json";
  }

  const res = await fetch(`${API_BASE}${url}`, {
    ...rest,
    headers,
    body: data instanceof FormData ? data : data ? JSON.stringify(data) : undefined,
  });

  if (res.status === 401) {
    if (typeof window !== "undefined") {
      localStorage.removeItem("token");
      window.location.href = "/login";
    }
    throw new ApiError(401, "Unauthorized");
  }
  if (!res.ok) {
    const err = await res.json().catch(() => ({}));
    throw new ApiError(res.status, err.detail || "Request failed");
  }
  return res.json();
}

export const api = {
  get: (url: string, opts?: Omit<RequestInit, "method">) => fetchApi(url, { ...opts, method: "GET" }),
  post: (url: string, data?: any, opts?: Omit<RequestInit, "method">) =>
    fetchApi(url, { ...opts, method: "POST", data }),
};
```

---

## 4.3 登录页 (login/page.tsx)

```tsx
"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import Link from "next/link";
import { api, ApiError } from "@/lib/api";

export default function LoginPage() {
  const router = useRouter();
  const [error, setError] = useState("");
  const [loading, setLoading] = useState(false);

  const handleSubmit = async (e: React.FormEvent<HTMLFormElement>) => {
    e.preventDefault();
    setError("");
    setLoading(true);
    const formData = new FormData(e.currentTarget);
    const username = formData.get("username");
    const password = formData.get("password");

    try {
      const params = new URLSearchParams();
      params.append("username", username as string);
      params.append("password", password as string);

      const data = await api.post("/api/auth/token", params, {
        headers: { "Content-Type": "application/x-www-form-urlencoded" },
      } as any);

      localStorage.setItem("token", data.access_token);
      router.push("/dashboard");
    } catch (err) {
      setError(err instanceof ApiError ? err.message : "Login failed");
    } finally {
      setLoading(false);
    }
  };

  return (
    <main className="min-h-screen flex items-center justify-center">
      <div className="w-full max-w-md p-8 border rounded-lg">
        <h1 className="text-2xl font-bold mb-6">登录</h1>
        <form onSubmit={handleSubmit} className="space-y-4">
          <div>
            <label className="block text-sm font-medium">用户名</label>
            <input name="username" type="text" required className="mt-1 block w-full border rounded px-3 py-2" />
          </div>
          <div>
            <label className="block text-sm font-medium">密码</label>
            <input name="password" type="password" required className="mt-1 block w-full border rounded px-3 py-2" />
          </div>
          {error && <p className="text-red-500 text-sm">{error}</p>}
          <button type="submit" disabled={loading} className="w-full bg-blue-600 text-white py-2 rounded">
            {loading ? "登录中..." : "登录"}
          </button>
        </form>
        <p className="mt-4 text-sm text-gray-600">
          没有账号？<Link href="/register" className="text-blue-600">注册</Link>
        </p>
      </div>
    </main>
  );
}
```

---

## 4.4 注册页 (register/page.tsx)

结构类似登录页，调用 `api.post("/api/auth/register", { email, username, password })`，成功后跳转登录或直接登录。

---

## 4.5 首页 (page.tsx)

```tsx
import Link from "next/link";

export default function Home() {
  return (
    <main className="min-h-screen p-8">
      <h1 className="text-4xl font-bold">RAG Web UI</h1>
      <p className="mt-4 text-gray-600">基于检索增强的知识库问答系统</p>
      <div className="mt-8 flex gap-4">
        <Link href="/login" className="px-4 py-2 bg-gray-200 rounded">登录</Link>
        <Link href="/register" className="px-4 py-2 bg-blue-600 text-white rounded">注册</Link>
      </div>
    </main>
  );
}
```

---

## 4.6 环境变量

在 `frontend` 根目录创建 `.env.local`：

```
NEXT_PUBLIC_API_URL=http://localhost:8000
```

---

## 4.7 验收标准

- [x] 首页可访问，有登录/注册入口
- [x] 注册后能创建账户
- [x] 登录后 Token 存 localStorage，跳转到 /dashboard（若尚无 dashboard 页面，可先跳 `/` 或显示 "登录成功"）
