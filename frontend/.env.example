# 复制为 .env.local 后本地开发（与 client.ts 默认一致，可不建文件）
NEXT_PUBLIC_API_URL=http://localhost:8000

# 可选：非流式请求超时（毫秒），范围 5000–3600000
# NEXT_PUBLIC_API_TIMEOUT_MS=120000

# --- Vercel（Dashboard → Settings → Environment Variables）---
# 名称：NEXT_PUBLIC_API_URL
# 本地联调若后端只在本机：线上预览/生产勿填 localhost（用户浏览器会连到用户自己的 8000 端口）。
# Production / Preview 应填公网 HTTPS 后端根地址，例如 https://api.your-domain.com
