# 认证模块

## 目录

- [模块概述](#模块概述)
- [API 端点](#api-端点)
- [核心实现](#核心实现)
- [调用链](#调用链)
- [关键代码](#关键代码)

## 模块概述

认证模块负责用户注册、登录和 JWT Token 管理，采用 OAuth2 密码模式。

**源码位置**: `app/modules/auth/`

## API 端点

| 方法 | 端点 | 功能 |
|------|------|------|
| POST | `/api/auth/register` | 用户注册 |
| POST | `/api/auth/token` | OAuth2 密码模式登录 |
| POST | `/api/auth/test-token` | 校验 Token（需 `Authorization: Bearer`） |

## 核心实现

### 注册流程

```
用户提交注册信息
    │
    ▼
bcrypt.hash_password(password)  // 密码哈希
    │
    ▼
UserRepository.create()  // 存入数据库
    │
    ▼
返回用户信息
```

### 登录流程

```
用户提交 username + password
    │
    ▼
UserRepository.get_by_username()  // 查询用户
    │
    ▼
security.verify_password()  // 验证密码
    │
    ▼
security.create_access_token(data={"sub": user.username})  // JWT 签发
    │
    ▼
返回 access_token + token_type
```

## 调用链

```
routes.py
  └── login_access_token_endpoint()
        │
        ▼
service.py
  └── login_access_token(db, username, password)
        │
        ├──► security.verify_password()
        │
        └──► security.create_access_token()
              │
              ▼
repository.py
  └── UserRepository.get_by_username()
```

## 关键代码

### security.py

```python
# JWT 签发
def create_access_token(data: dict) -> str:
    to_encode = data.copy()
    expire = datetime.utcnow() + timedelta(minutes=ACCESS_TOKEN_EXPIRE_MINUTES)
    to_encode.update({"exp": expire})
    encoded_jwt = jwt.encode(to_encode, SECRET_KEY, algorithm=ALGORITHM)
    return encoded_jwt

# 密码验证
def verify_password(plain_password: str, hashed_password: str) -> bool:
    return pwd_context.verify(plain_password, hashed_password)

# 密码哈希
def get_password_hash(password: str) -> str:
    return pwd_context.hash(password)
```

### deps.py (依赖注入)

```python
# 获取当前用户
async def get_current_user(token: Annotated[str, Depends(JWTBearer())]):
    credentials_exception = HTTPException(...)
    try:
        payload = jwt.decode(token, SECRET_KEY, algorithms=[ALGORITHM])
        username: str = payload.get("sub")
        if username is None:
            raise credentials_exception
    except JWTError:
        raise credentials_exception

    user = db.query(User).filter(User.username == username).first()
    if user is None:
        raise credentials_exception
    return user
```
